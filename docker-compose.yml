
services:
  # ── Application (frontend + API) ──────────────────────────────
  app:
    build: .
    container_name: frontend-course-orientation
    restart: unless-stopped
    ports:
      - "80:8787"
    env_file: .env.docker
