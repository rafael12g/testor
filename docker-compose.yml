
services:
  # ── Application (frontend + API) ──────────────────────────────
  app:
    build: .
    container_name: testor-app
    restart: unless-stopped
    ports:
      - "80:8787"
    env_file: .env.docker
