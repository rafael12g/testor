# Backend SQL provider: sqlite (default) or mysql
DB_PROVIDER=mysql

# MySQL config for phpMyAdmin stack
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=orienteering
DB_PASSWORD=orienteering
DB_NAME=orienteering

# API server port
PORT=8787
